# Epic Title: Persist Data with PostgreSQL for Shopping Cart and Wishlist

Given the requirement to switch from MySQL to PostgreSQL for the persistence of shopping cart and wishlist data, we need to modify the database connection setup accordingly. The following files will be created or modified to adapt to PostgreSQL.

The backend structure provided will have the necessary models, routes, and schemas in line with the user stories.