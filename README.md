# Cartly

Environment Variables required to run the repository

APP_NAME=Cartly APP_ENV=development APP_URL=http://localhost:3000 APP_PORT=3000 APP_SECRET=supersecretkey DB_HOST=localhost DB_PORT=5432 DB_NAME=cartly_db DB_USER=cartly_user DB_PASSWORD=strongpassword
